<UserControl
    x:Class="PicturesAndNotes.InkColor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PicturesAndNotes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Margin="7.5" VerticalAlignment="Stretch">
        <Border x:ConnectionId='1' x:Name="Target" Background="{Binding BackgroundColor}" 
                BorderThickness="{Binding BorderSize}" 
                BorderBrush="{Binding BorderColor}" 
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                                                        
                CornerRadius="15.0">
        </Border>
    </Grid>
</UserControl>

